# Keel v0.1 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Go binary that runs agent loops (`keel run <agent>`) and bridges Discord channels to agent directories (`keel serve --discord`).

**Architecture:** Single Go binary with cobra CLI. A shared `LoopManager` runs one goroutine per agent — each loop checks GOALS.md, optionally runs nark calibration, invokes `claude --agent <name> --permission-mode dontAsk -p "$(cat PROGRAM.md)"`, and sleeps before repeating. The Discord bot maps channels to agent dirs via TOML config, appends messages to GOALS.md, tails log.md with fsnotify, and handles `!` commands. The filesystem is the protocol — no MCP, no custom IPC.

**Tech Stack:** Go 1.26, cobra (CLI), discordgo (Discord), BurntSushi/toml (config), fsnotify (file watching)

---

## File Structure

```
keel/
├── main.go                     # Entry point, calls cmd.Execute()
├── cmd/
│   ├── root.go                 # Root cobra command + global flags
│   ├── run.go                  # `keel run <agent>` — single agent CLI loop
│   ├── serve.go                # `keel serve --discord` — Discord bot + loops
│   └── status.go               # `keel status <agent>` — quick status check
├── internal/
│   ├── agent/
│   │   ├── agent.go            # Agent struct: reads workspace files (GOALS, MEMORY, PROGRAM, etc.)
│   │   └── agent_test.go
│   ├── loop/
│   │   ├── loop.go             # AgentLoop: runs one agent's while-loop (runOnce, calibrate)
│   │   ├── manager.go          # LoopManager: tracks multiple AgentLoops, Start/Stop/IsRunning
│   │   └── loop_test.go
│   ├── config/
│   │   ├── config.go           # TOML config types + Load()
│   │   └── config_test.go
│   ├── discord/
│   │   ├── bot.go              # Discord bot: connect, message handler, command router
│   │   ├── commands.go         # ! command handlers (!status, !goals, !log, !stop, etc.)
│   │   ├── tail.go             # LogTailer: fsnotify on log.md, posts new lines to channel
│   │   └── bot_test.go
│   └── workspace/
│       ├── workspace.go        # File I/O helpers: ReadGoals, AppendGoal, ReadLog, ReadMemory, etc.
│       └── workspace_test.go
├── config/
│   └── discord.example.toml    # Example config
├── go.mod
└── go.sum
```

---

## Chunk 1: Project Scaffold + Config + Agent Workspace

### Task 1: Initialize Go module and dependencies

**Files:**
- Create: `go.mod`
- Create: `main.go`

- [ ] **Step 1: Init Go module**

```bash
cd /Users/seanochang/dev/projects/agents/keel
go mod init github.com/SeanoChang/keel
```

- [ ] **Step 2: Add dependencies**

```bash
go get github.com/spf13/cobra@latest
go get github.com/BurntSushi/toml@latest
go get github.com/bwmarrin/discordgo@latest
go get github.com/fsnotify/fsnotify@latest
```

- [ ] **Step 3: Create main.go**

```go
// main.go
package main

import "github.com/SeanoChang/keel/cmd"

func main() {
	cmd.Execute()
}
```

- [ ] **Step 4: Create root command**

```go
// cmd/root.go
package cmd

import (
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

var rootCmd = &cobra.Command{
	Use:   "keel",
	Short: "Agent loop manager and Discord bridge",
}

func Execute() {
	if err := rootCmd.Execute(); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}
```

- [ ] **Step 5: Verify it compiles**

Run: `go build -o keel . && ./keel --help`
Expected: Shows "Agent loop manager and Discord bridge" usage text.

- [ ] **Step 6: Commit**

```bash
git add main.go cmd/root.go go.mod go.sum
git commit -m "feat: init Go module with cobra root command"
```

---

### Task 2: Config loading

**Files:**
- Create: `internal/config/config.go`
- Create: `internal/config/config_test.go`
- Create: `config/discord.example.toml`

- [ ] **Step 1: Write config test**

```go
// internal/config/config_test.go
package config

import (
	"os"
	"path/filepath"
	"testing"
)

func TestLoadConfig(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "discord.toml")
	content := `
[bot]
token_env = "DISCORD_BOT_TOKEN"
guild_id = "123456789"
status_channel_id = ""

[channels.noah]
channel_id = "111111111"
agent_dir = "/tmp/test-ark/noah"
`
	os.WriteFile(path, []byte(content), 0644)

	cfg, err := Load(path)
	if err != nil {
		t.Fatalf("Load() error: %v", err)
	}
	if cfg.Bot.TokenEnv != "DISCORD_BOT_TOKEN" {
		t.Errorf("TokenEnv = %q, want DISCORD_BOT_TOKEN", cfg.Bot.TokenEnv)
	}
	if cfg.Bot.GuildID != "123456789" {
		t.Errorf("GuildID = %q, want 123456789", cfg.Bot.GuildID)
	}
	ch, ok := cfg.Channels["noah"]
	if !ok {
		t.Fatal("missing channel noah")
	}
	if ch.ChannelID != "111111111" {
		t.Errorf("ChannelID = %q, want 111111111", ch.ChannelID)
	}
	if ch.AgentDir != "/tmp/test-ark/noah" {
		t.Errorf("AgentDir = %q, want /tmp/test-ark/noah", ch.AgentDir)
	}
}

func TestLoadConfigMissingFile(t *testing.T) {
	_, err := Load("/nonexistent/path.toml")
	if err == nil {
		t.Fatal("expected error for missing file")
	}
}

func TestResolveChannelByID(t *testing.T) {
	cfg := &Config{
		Channels: map[string]ChannelConfig{
			"noah": {ChannelID: "111", AgentDir: "/tmp/noah"},
		},
	}
	name, ch, ok := cfg.ResolveChannel("111")
	if !ok {
		t.Fatal("expected to find channel")
	}
	if name != "noah" {
		t.Errorf("name = %q, want noah", name)
	}
	if ch.AgentDir != "/tmp/noah" {
		t.Errorf("AgentDir = %q", ch.AgentDir)
	}

	_, _, ok = cfg.ResolveChannel("999")
	if ok {
		t.Fatal("expected no match for unknown channel ID")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/config/ -v`
Expected: FAIL — types not defined yet.

- [ ] **Step 3: Implement config**

```go
// internal/config/config.go
package config

import (
	"fmt"
	"os"

	"github.com/BurntSushi/toml"
)

type BotConfig struct {
	TokenEnv        string `toml:"token_env"`
	GuildID         string `toml:"guild_id"`
	StatusChannelID string `toml:"status_channel_id"`
}

type ChannelConfig struct {
	ChannelID string `toml:"channel_id"`
	AgentDir  string `toml:"agent_dir"`
}

type Config struct {
	Bot      BotConfig                `toml:"bot"`
	Channels map[string]ChannelConfig `toml:"channels"`
}

// ResolveChannel finds a channel config by Discord channel ID.
// Returns the agent name, config, and whether it was found.
func (c *Config) ResolveChannel(discordChannelID string) (string, ChannelConfig, bool) {
	for name, ch := range c.Channels {
		if ch.ChannelID == discordChannelID {
			return name, ch, true
		}
	}
	return "", ChannelConfig{}, false
}

func Load(path string) (*Config, error) {
	data, err := os.ReadFile(path)
	if err != nil {
		return nil, fmt.Errorf("read config: %w", err)
	}
	var cfg Config
	if err := toml.Unmarshal(data, &cfg); err != nil {
		return nil, fmt.Errorf("parse config: %w", err)
	}
	return &cfg, nil
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/config/ -v`
Expected: PASS

- [ ] **Step 5: Create example config**

```toml
# config/discord.example.toml

[bot]
token_env = "DISCORD_BOT_TOKEN"    # env var name containing the bot token
guild_id = "YOUR_GUILD_ID"
status_channel_id = ""              # optional: broadcast channel for all agents

[channels.noah]
channel_id = "YOUR_CHANNEL_ID"
agent_dir = "~/.ark/noah"
```

- [ ] **Step 6: Commit**

```bash
git add internal/config/ config/
git commit -m "feat: TOML config loading with channel resolution"
```

---

### Task 3: Workspace file helpers

**Files:**
- Create: `internal/workspace/workspace.go`
- Create: `internal/workspace/workspace_test.go`

- [ ] **Step 1: Write workspace tests**

```go
// internal/workspace/workspace_test.go
package workspace

import (
	"os"
	"path/filepath"
	"strings"
	"testing"
)

func setupWorkspace(t *testing.T) string {
	t.Helper()
	dir := t.TempDir()
	os.WriteFile(filepath.Join(dir, "GOALS.md"), []byte("## Goal 1\nDo something\n"), 0644)
	os.WriteFile(filepath.Join(dir, "MEMORY.md"), []byte("Some memory content here\n"), 0644)
	os.WriteFile(filepath.Join(dir, "log.md"), []byte("- did thing 1\n- did thing 2\n"), 0644)
	os.WriteFile(filepath.Join(dir, "PROGRAM.md"), []byte("Work on goals. Exit when done.\n"), 0644)
	return dir
}

func TestReadGoals(t *testing.T) {
	dir := setupWorkspace(t)
	goals, err := ReadGoals(dir)
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(goals, "Goal 1") {
		t.Errorf("goals missing expected content: %q", goals)
	}
}

func TestHasGoals(t *testing.T) {
	dir := setupWorkspace(t)
	if !HasGoals(dir) {
		t.Error("expected HasGoals=true")
	}
	os.WriteFile(filepath.Join(dir, "GOALS.md"), []byte(""), 0644)
	if HasGoals(dir) {
		t.Error("expected HasGoals=false for empty file")
	}
	os.Remove(filepath.Join(dir, "GOALS.md"))
	if HasGoals(dir) {
		t.Error("expected HasGoals=false for missing file")
	}
}

func TestAppendGoal(t *testing.T) {
	dir := setupWorkspace(t)
	err := AppendGoal(dir, "testuser", "Research Wyckoff structures")
	if err != nil {
		t.Fatal(err)
	}
	goals, _ := ReadGoals(dir)
	if !strings.Contains(goals, "Research Wyckoff structures") {
		t.Error("appended goal not found")
	}
	if !strings.Contains(goals, "from testuser") {
		t.Error("username not found in goal header")
	}
}

func TestReadProgram(t *testing.T) {
	dir := setupWorkspace(t)
	program, err := ReadProgram(dir)
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(program, "Work on goals") {
		t.Errorf("unexpected program: %q", program)
	}
}

func TestReadProgramDefault(t *testing.T) {
	dir := t.TempDir() // no PROGRAM.md
	program, err := ReadProgram(dir)
	if err != nil {
		t.Fatal(err)
	}
	if program == "" {
		t.Error("expected default program, got empty")
	}
}

func TestReadLogTail(t *testing.T) {
	dir := setupWorkspace(t)
	lines, err := ReadLogTail(dir, 1)
	if err != nil {
		t.Fatal(err)
	}
	if len(lines) != 1 {
		t.Errorf("expected 1 line, got %d", len(lines))
	}
}

func TestMemoryTokenCount(t *testing.T) {
	dir := setupWorkspace(t)
	count, err := MemoryTokenCount(dir)
	if err != nil {
		t.Fatal(err)
	}
	if count == 0 {
		t.Error("expected non-zero token count")
	}
}

func TestClearGoals(t *testing.T) {
	dir := setupWorkspace(t)
	err := ClearGoals(dir)
	if err != nil {
		t.Fatal(err)
	}
	if HasGoals(dir) {
		t.Error("expected empty goals after clear")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/workspace/ -v`
Expected: FAIL — functions not defined.

- [ ] **Step 3: Implement workspace**

```go
// internal/workspace/workspace.go
package workspace

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"
)

const defaultProgram = "Read GOALS.md. Work on the highest-priority goal. Exit when done."

func ReadGoals(dir string) (string, error) {
	data, err := os.ReadFile(filepath.Join(dir, "GOALS.md"))
	if err != nil {
		return "", err
	}
	return string(data), nil
}

func HasGoals(dir string) bool {
	data, err := os.ReadFile(filepath.Join(dir, "GOALS.md"))
	if err != nil {
		return false
	}
	return len(strings.TrimSpace(string(data))) > 0
}

func AppendGoal(dir, username, message string) error {
	f, err := os.OpenFile(filepath.Join(dir, "GOALS.md"), os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0644)
	if err != nil {
		return err
	}
	defer f.Close()
	ts := time.Now().Format("2006-01-02 15:04")
	_, err = fmt.Fprintf(f, "\n## [%s] from %s\n%s\n", ts, username, message)
	return err
}

func ClearGoals(dir string) error {
	return os.WriteFile(filepath.Join(dir, "GOALS.md"), []byte(""), 0644)
}

func ReadProgram(dir string) (string, error) {
	data, err := os.ReadFile(filepath.Join(dir, "PROGRAM.md"))
	if err != nil {
		if os.IsNotExist(err) {
			return defaultProgram, nil
		}
		return "", err
	}
	s := strings.TrimSpace(string(data))
	if s == "" {
		return defaultProgram, nil
	}
	return s, nil
}

func ReadLogTail(dir string, n int) ([]string, error) {
	data, err := os.ReadFile(filepath.Join(dir, "log.md"))
	if err != nil {
		if os.IsNotExist(err) {
			return nil, nil
		}
		return "", err
	}
	lines := strings.Split(strings.TrimSpace(string(data)), "\n")
	if n >= len(lines) {
		return lines, nil
	}
	return lines[len(lines)-n:], nil
}

func ReadMemory(dir string) (string, error) {
	data, err := os.ReadFile(filepath.Join(dir, "MEMORY.md"))
	if err != nil {
		if os.IsNotExist(err) {
			return "", nil
		}
		return "", err
	}
	return string(data), nil
}

// MemoryTokenCount returns approximate token count (words ≈ 0.75 tokens, so word count * 4/3).
func MemoryTokenCount(dir string) (int, error) {
	content, err := ReadMemory(dir)
	if err != nil {
		return 0, err
	}
	words := len(strings.Fields(content))
	return words * 4 / 3, nil
}

func LogSize(dir string) (int64, error) {
	info, err := os.Stat(filepath.Join(dir, "log.md"))
	if err != nil {
		if os.IsNotExist(err) {
			return 0, nil
		}
		return 0, err
	}
	return info.Size(), nil
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/workspace/ -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/workspace/
git commit -m "feat: workspace file helpers for agent directory I/O"
```

---

## Chunk 2: Agent Loop + `keel run`

### Task 4: Agent struct

**Files:**
- Create: `internal/agent/agent.go`
- Create: `internal/agent/agent_test.go`

- [ ] **Step 1: Write agent test**

```go
// internal/agent/agent_test.go
package agent

import (
	"os"
	"path/filepath"
	"testing"
)

func setupAgentDir(t *testing.T) string {
	t.Helper()
	dir := t.TempDir()
	os.WriteFile(filepath.Join(dir, "GOALS.md"), []byte("## Goal\nDo work\n"), 0644)
	os.WriteFile(filepath.Join(dir, "PROGRAM.md"), []byte("Work hard.\n"), 0644)
	os.WriteFile(filepath.Join(dir, "MEMORY.md"), []byte("I remember things.\n"), 0644)
	os.MkdirAll(filepath.Join(dir, ".claude", "agents"), 0755)
	os.WriteFile(filepath.Join(dir, ".claude", "agents", "test.md"), []byte("---\nname: test\n---\n"), 0644)
	return dir
}

func TestNewAgent(t *testing.T) {
	dir := setupAgentDir(t)
	a, err := New("test", dir)
	if err != nil {
		t.Fatal(err)
	}
	if a.Name != "test" {
		t.Errorf("Name = %q", a.Name)
	}
	if a.Dir != dir {
		t.Errorf("Dir = %q", a.Dir)
	}
}

func TestNewAgentMissingDir(t *testing.T) {
	_, err := New("test", "/nonexistent/path")
	if err == nil {
		t.Fatal("expected error for missing dir")
	}
}

func TestAgentHasGoals(t *testing.T) {
	dir := setupAgentDir(t)
	a, _ := New("test", dir)
	if !a.HasGoals() {
		t.Error("expected HasGoals=true")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/agent/ -v`
Expected: FAIL

- [ ] **Step 3: Implement agent**

```go
// internal/agent/agent.go
package agent

import (
	"fmt"
	"os"

	"github.com/SeanoChang/keel/internal/workspace"
)

type Agent struct {
	Name string
	Dir  string
}

func New(name, dir string) (*Agent, error) {
	info, err := os.Stat(dir)
	if err != nil {
		return nil, fmt.Errorf("agent dir %q: %w", dir, err)
	}
	if !info.IsDir() {
		return nil, fmt.Errorf("agent dir %q is not a directory", dir)
	}
	return &Agent{Name: name, Dir: dir}, nil
}

func (a *Agent) HasGoals() bool {
	return workspace.HasGoals(a.Dir)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/agent/ -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/agent/
git commit -m "feat: agent struct wrapping workspace directory"
```

---

### Task 5: Agent loop

**Files:**
- Create: `internal/loop/loop.go`
- Create: `internal/loop/manager.go`
- Create: `internal/loop/loop_test.go`

- [ ] **Step 1: Write loop test**

Tests use a mock command instead of real `claude` to verify the loop mechanics.

```go
// internal/loop/loop_test.go
package loop

import (
	"context"
	"os"
	"path/filepath"
	"sync"
	"testing"
	"time"
)

func setupTestAgent(t *testing.T) string {
	t.Helper()
	dir := t.TempDir()
	os.WriteFile(filepath.Join(dir, "GOALS.md"), []byte("## Goal\nTest\n"), 0644)
	os.WriteFile(filepath.Join(dir, "PROGRAM.md"), []byte("Do the thing.\n"), 0644)
	os.WriteFile(filepath.Join(dir, "log.md"), []byte(""), 0644)
	os.MkdirAll(filepath.Join(dir, ".claude", "agents"), 0755)
	os.WriteFile(filepath.Join(dir, ".claude", "agents", "test.md"), []byte("---\nname: test\n---\n"), 0644)
	return dir
}

func TestRunOnceExecutesCommand(t *testing.T) {
	dir := setupTestAgent(t)

	// Use a script that clears GOALS.md to simulate agent completing work
	loop := &AgentLoop{
		Name: "test",
		Dir:  dir,
		// Override command builder for testing
		CommandBuilder: func(ctx context.Context, name, dir, program string) *CommandSpec {
			return &CommandSpec{
				Name: "bash",
				Args: []string{"-c", "echo '' > " + filepath.Join(dir, "GOALS.md")},
				Dir:  dir,
			}
		},
		SleepDuration: 100 * time.Millisecond,
	}

	err := loop.RunOnce(context.Background())
	if err != nil {
		t.Fatalf("RunOnce error: %v", err)
	}

	// Verify GOALS.md was cleared by the "agent"
	data, _ := os.ReadFile(filepath.Join(dir, "GOALS.md"))
	if len(data) > 1 { // allow trailing newline
		t.Errorf("GOALS.md should be empty, got: %q", string(data))
	}
}

func TestLoopStopsWhenGoalsEmpty(t *testing.T) {
	dir := setupTestAgent(t)

	var runs int
	var mu sync.Mutex

	loop := &AgentLoop{
		Name: "test",
		Dir:  dir,
		CommandBuilder: func(ctx context.Context, name, dir, program string) *CommandSpec {
			mu.Lock()
			runs++
			mu.Unlock()
			// Clear goals on first run
			os.WriteFile(filepath.Join(dir, "GOALS.md"), []byte(""), 0644)
			return &CommandSpec{
				Name: "true", // no-op
				Args: nil,
				Dir:  dir,
			}
		},
		SleepDuration: 50 * time.Millisecond,
	}

	ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
	defer cancel()

	loop.Run(ctx)

	mu.Lock()
	defer mu.Unlock()
	if runs != 1 {
		t.Errorf("expected 1 run, got %d", runs)
	}
}

func TestManagerStartStop(t *testing.T) {
	dir := setupTestAgent(t)

	mgr := NewManager()

	// Use a command that sleeps so the loop stays alive
	builder := func(ctx context.Context, name, dir, program string) *CommandSpec {
		return &CommandSpec{
			Name: "sleep",
			Args: []string{"10"},
			Dir:  dir,
		}
	}

	err := mgr.Start("test", dir, builder, 100*time.Millisecond)
	if err != nil {
		t.Fatal(err)
	}
	if !mgr.IsRunning("test") {
		t.Error("expected loop to be running")
	}

	// Starting same agent again should error
	err = mgr.Start("test", dir, builder, 100*time.Millisecond)
	if err == nil {
		t.Error("expected error when starting duplicate")
	}

	mgr.Stop("test")
	// Give goroutine time to exit
	time.Sleep(200 * time.Millisecond)
	if mgr.IsRunning("test") {
		t.Error("expected loop to be stopped")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/loop/ -v`
Expected: FAIL

- [ ] **Step 3: Implement loop and manager**

```go
// internal/loop/loop.go
package loop

import (
	"context"
	"fmt"
	"log"
	"os/exec"
	"time"

	"github.com/SeanoChang/keel/internal/workspace"
)

// CommandSpec describes a command to execute. Used for testing (swap real claude for mocks).
type CommandSpec struct {
	Name string
	Args []string
	Dir  string
	Env  []string
}

// CommandBuilder creates a CommandSpec for a given agent. In production this builds the
// `claude --agent <name> ...` invocation. Tests replace it with mocks.
type CommandBuilder func(ctx context.Context, name, dir, program string) *CommandSpec

// DefaultCommandBuilder returns the real claude CLI invocation.
func DefaultCommandBuilder(ctx context.Context, name, dir, program string) *CommandSpec {
	return &CommandSpec{
		Name: "claude",
		Args: []string{
			"--agent", name,
			"--permission-mode", "dontAsk",
			"-p", program,
		},
		Dir: dir,
	}
}

type AgentLoop struct {
	Name           string
	Dir            string
	CommandBuilder CommandBuilder
	SleepDuration  time.Duration
	OnOutput       func(line string) // optional callback for log lines
}

// RunOnce executes one agent session.
func (l *AgentLoop) RunOnce(ctx context.Context) error {
	program, err := workspace.ReadProgram(l.Dir)
	if err != nil {
		return fmt.Errorf("read program: %w", err)
	}

	spec := l.CommandBuilder(ctx, l.Name, l.Dir, program)
	cmd := exec.CommandContext(ctx, spec.Name, spec.Args...)
	cmd.Dir = spec.Dir
	if spec.Env != nil {
		cmd.Env = spec.Env
	}

	output, err := cmd.CombinedOutput()
	if l.OnOutput != nil && len(output) > 0 {
		l.OnOutput(string(output))
	}
	if err != nil {
		if ctx.Err() != nil {
			return ctx.Err()
		}
		return fmt.Errorf("agent %s exited: %w", l.Name, err)
	}
	return nil
}

// Run loops: check goals → runOnce → sleep → repeat. Exits when goals empty or ctx cancelled.
func (l *AgentLoop) Run(ctx context.Context) {
	for {
		if ctx.Err() != nil {
			return
		}
		if !workspace.HasGoals(l.Dir) {
			log.Printf("[keel] %s: no goals, loop exiting", l.Name)
			return
		}

		log.Printf("[keel] %s: starting session", l.Name)
		err := l.RunOnce(ctx)
		if err != nil {
			if ctx.Err() != nil {
				return
			}
			log.Printf("[keel] %s: session error: %v", l.Name, err)
		}

		select {
		case <-ctx.Done():
			return
		case <-time.After(l.SleepDuration):
		}
	}
}
```

```go
// internal/loop/manager.go
package loop

import (
	"context"
	"fmt"
	"sync"
	"time"
)

type runningLoop struct {
	cancel context.CancelFunc
	done   chan struct{}
}

type Manager struct {
	mu    sync.Mutex
	loops map[string]*runningLoop
}

func NewManager() *Manager {
	return &Manager{
		loops: make(map[string]*runningLoop),
	}
}

func (m *Manager) Start(name, dir string, builder CommandBuilder, sleep time.Duration) error {
	m.mu.Lock()
	defer m.mu.Unlock()

	if rl, ok := m.loops[name]; ok {
		select {
		case <-rl.done:
			delete(m.loops, name)
		default:
			return fmt.Errorf("agent %s is already running", name)
		}
	}

	ctx, cancel := context.WithCancel(context.Background())
	done := make(chan struct{})

	loop := &AgentLoop{
		Name:           name,
		Dir:            dir,
		CommandBuilder: builder,
		SleepDuration:  sleep,
	}

	m.loops[name] = &runningLoop{cancel: cancel, done: done}

	go func() {
		defer close(done)
		loop.Run(ctx)
	}()

	return nil
}

func (m *Manager) Stop(name string) {
	m.mu.Lock()
	rl, ok := m.loops[name]
	m.mu.Unlock()

	if ok {
		rl.cancel()
		<-rl.done
		m.mu.Lock()
		delete(m.loops, name)
		m.mu.Unlock()
	}
}

func (m *Manager) IsRunning(name string) bool {
	m.mu.Lock()
	defer m.mu.Unlock()

	rl, ok := m.loops[name]
	if !ok {
		return false
	}
	select {
	case <-rl.done:
		delete(m.loops, name)
		return false
	default:
		return true
	}
}

func (m *Manager) StopAll() {
	m.mu.Lock()
	names := make([]string, 0, len(m.loops))
	for name := range m.loops {
		names = append(names, name)
	}
	m.mu.Unlock()

	for _, name := range names {
		m.Stop(name)
	}
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/loop/ -v -timeout 10s`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/loop/
git commit -m "feat: agent loop and manager with goroutine-per-agent"
```

---

### Task 6: `keel run <agent>` command

**Files:**
- Create: `cmd/run.go`

- [ ] **Step 1: Implement run command**

```go
// cmd/run.go
package cmd

import (
	"context"
	"fmt"
	"log"
	"os"
	"os/signal"
	"path/filepath"
	"strings"
	"syscall"
	"time"

	"github.com/SeanoChang/keel/internal/agent"
	"github.com/SeanoChang/keel/internal/loop"
	"github.com/SeanoChang/keel/internal/workspace"
	"github.com/spf13/cobra"
)

var (
	runAgentDir string
	runSleep    time.Duration
)

var runCmd = &cobra.Command{
	Use:   "run <agent>",
	Short: "Run an agent loop until GOALS.md is empty",
	Args:  cobra.ExactArgs(1),
	RunE:  runRun,
}

func init() {
	runCmd.Flags().StringVar(&runAgentDir, "dir", "", "Agent directory (default: ~/.ark/<agent>)")
	runCmd.Flags().DurationVar(&runSleep, "sleep", 5*time.Second, "Sleep between sessions")
	rootCmd.AddCommand(runCmd)
}

func resolveAgentDir(name string) string {
	if runAgentDir != "" {
		if strings.HasPrefix(runAgentDir, "~") {
			home, _ := os.UserHomeDir()
			return filepath.Join(home, runAgentDir[1:])
		}
		return runAgentDir
	}
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".ark", name)
}

func runRun(cmd *cobra.Command, args []string) error {
	name := args[0]
	dir := resolveAgentDir(name)

	a, err := agent.New(name, dir)
	if err != nil {
		return fmt.Errorf("agent %s: %w", name, err)
	}

	if !a.HasGoals() {
		log.Printf("[keel] %s: no goals in %s/GOALS.md, nothing to do", name, dir)
		return nil
	}

	goals, _ := workspace.ReadGoals(dir)
	log.Printf("[keel] %s: starting loop (%s)", name, dir)
	log.Printf("[keel] goals:\n%s", goals)

	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()

	sig := make(chan os.Signal, 1)
	signal.Notify(sig, syscall.SIGINT, syscall.SIGTERM)
	go func() {
		<-sig
		log.Printf("[keel] %s: shutting down...", name)
		cancel()
	}()

	l := &loop.AgentLoop{
		Name:           name,
		Dir:            dir,
		CommandBuilder: loop.DefaultCommandBuilder,
		SleepDuration:  runSleep,
	}
	l.Run(ctx)

	log.Printf("[keel] %s: loop finished", name)
	return nil
}
```

- [ ] **Step 2: Verify it compiles and shows help**

Run: `go build -o keel . && ./keel run --help`
Expected: Shows usage for `keel run <agent>` with `--dir` and `--sleep` flags.

- [ ] **Step 3: Commit**

```bash
git add cmd/run.go
git commit -m "feat: keel run command — CLI agent loop"
```

---

### Task 7: `keel status <agent>` command

**Files:**
- Create: `cmd/status.go`

- [ ] **Step 1: Implement status command**

```go
// cmd/status.go
package cmd

import (
	"fmt"
	"strings"

	"github.com/SeanoChang/keel/internal/agent"
	"github.com/SeanoChang/keel/internal/workspace"
	"github.com/spf13/cobra"
)

var statusCmd = &cobra.Command{
	Use:   "status <agent>",
	Short: "Show agent status: goals, memory token count, log tail",
	Args:  cobra.ExactArgs(1),
	RunE:  runStatus,
}

func init() {
	statusCmd.Flags().StringVar(&runAgentDir, "dir", "", "Agent directory (default: ~/.ark/<agent>)")
	rootCmd.AddCommand(statusCmd)
}

func runStatus(cmd *cobra.Command, args []string) error {
	name := args[0]
	dir := resolveAgentDir(name)

	_, err := agent.New(name, dir)
	if err != nil {
		return err
	}

	// Goals
	goals, err := workspace.ReadGoals(dir)
	if err != nil {
		goals = "(no GOALS.md)"
	}
	hasGoals := workspace.HasGoals(dir)

	// Memory
	tokens, _ := workspace.MemoryTokenCount(dir)

	// Log tail
	logLines, _ := workspace.ReadLogTail(dir, 5)

	fmt.Printf("Agent: %s\n", name)
	fmt.Printf("Dir:   %s\n", dir)
	fmt.Printf("Goals: %v\n", hasGoals)
	if hasGoals {
		fmt.Printf("\n--- GOALS.md ---\n%s\n", strings.TrimSpace(goals))
	}
	fmt.Printf("\nMEMORY.md: ~%d tokens\n", tokens)
	if len(logLines) > 0 {
		fmt.Printf("\n--- log.md (last %d) ---\n", len(logLines))
		for _, line := range logLines {
			fmt.Println(line)
		}
	}
	return nil
}
```

- [ ] **Step 2: Verify it compiles**

Run: `go build -o keel . && ./keel status --help`
Expected: Shows usage for `keel status <agent>`.

- [ ] **Step 3: Commit**

```bash
git add cmd/status.go
git commit -m "feat: keel status command — quick agent status check"
```

---

## Chunk 3: Discord Bot

### Task 8: Discord bot core

**Files:**
- Create: `internal/discord/bot.go`
- Create: `internal/discord/commands.go`
- Create: `internal/discord/tail.go`
- Create: `internal/discord/bot_test.go`

- [ ] **Step 1: Write bot test**

The Discord bot is hard to unit test in isolation (requires live Discord connection), so we test the message parsing and command routing logic.

```go
// internal/discord/bot_test.go
package discord

import (
	"testing"
)

func TestParseCommand(t *testing.T) {
	tests := []struct {
		input   string
		isCmd   bool
		cmd     string
		args    string
	}{
		{"!status", true, "status", ""},
		{"!goals", true, "goals", ""},
		{"!log 10", true, "log", "10"},
		{"!stop", true, "stop", ""},
		{"hello world", false, "", ""},
		{"", false, "", ""},
		{"! ", true, "", ""},
	}
	for _, tt := range tests {
		isCmd, cmd, args := ParseCommand(tt.input)
		if isCmd != tt.isCmd {
			t.Errorf("ParseCommand(%q) isCmd = %v, want %v", tt.input, isCmd, tt.isCmd)
		}
		if cmd != tt.cmd {
			t.Errorf("ParseCommand(%q) cmd = %q, want %q", tt.input, cmd, tt.cmd)
		}
		if args != tt.args {
			t.Errorf("ParseCommand(%q) args = %q, want %q", tt.input, args, tt.args)
		}
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/discord/ -v`
Expected: FAIL

- [ ] **Step 3: Implement bot**

```go
// internal/discord/bot.go
package discord

import (
	"fmt"
	"log"
	"strings"
	"time"

	"github.com/bwmarrin/discordgo"

	"github.com/SeanoChang/keel/internal/config"
	"github.com/SeanoChang/keel/internal/loop"
	"github.com/SeanoChang/keel/internal/workspace"
)

type Bot struct {
	session     *discordgo.Session
	cfg         *config.Config
	loopMgr     *loop.Manager
	tailers     map[string]*LogTailer // keyed by agent name
	sleepBetween time.Duration
}

func NewBot(cfg *config.Config, sleepBetween time.Duration) (*Bot, error) {
	token := lookupToken(cfg.Bot.TokenEnv)
	if token == "" {
		return nil, fmt.Errorf("env var %s is empty or unset", cfg.Bot.TokenEnv)
	}

	session, err := discordgo.New("Bot " + token)
	if err != nil {
		return nil, fmt.Errorf("create discord session: %w", err)
	}

	b := &Bot{
		session:      session,
		cfg:          cfg,
		loopMgr:      loop.NewManager(),
		tailers:      make(map[string]*LogTailer),
		sleepBetween: sleepBetween,
	}

	session.AddHandler(b.onMessageCreate)
	session.Identify.Intents = discordgo.IntentsGuildMessages | discordgo.IntentsMessageContent

	return b, nil
}

func lookupToken(envVar string) string {
	if envVar == "" {
		return ""
	}
	// Support both env var name and direct token (for testing)
	val := strings.TrimSpace(envVar)
	if !strings.HasPrefix(val, "Bot ") && len(val) < 100 {
		// Looks like an env var name, resolve it
		import_os_val := osLookupEnv(val)
		if import_os_val != "" {
			return import_os_val
		}
	}
	return val
}

func osLookupEnv(key string) string {
	import "os"
	return os.Getenv(key)
}

func (b *Bot) Start() error {
	if err := b.session.Open(); err != nil {
		return fmt.Errorf("open discord connection: %w", err)
	}
	log.Printf("[keel] Discord bot connected")

	// Start tailers for all configured channels
	for name, ch := range b.cfg.Channels {
		tailer := NewLogTailer(name, ch.AgentDir, ch.ChannelID, b.session)
		b.tailers[name] = tailer
		go tailer.Start()
	}

	return nil
}

func (b *Bot) Stop() {
	b.loopMgr.StopAll()
	for _, t := range b.tailers {
		t.Stop()
	}
	b.session.Close()
	log.Printf("[keel] Discord bot disconnected")
}

func (b *Bot) onMessageCreate(s *discordgo.Session, m *discordgo.MessageCreate) {
	// Ignore own messages
	if m.Author.ID == s.State.User.ID {
		return
	}

	// Check if this channel is mapped
	agentName, ch, ok := b.cfg.ResolveChannel(m.ChannelID)
	if !ok {
		return
	}

	content := strings.TrimSpace(m.Content)
	if content == "" {
		return
	}

	// Check for ! commands
	if isCmd, cmd, args := ParseCommand(content); isCmd {
		b.handleCommand(s, m, agentName, ch, cmd, args)
		return
	}

	// Regular message → append to GOALS.md
	username := m.Author.Username
	if err := workspace.AppendGoal(ch.AgentDir, username, content); err != nil {
		b.reply(s, m, fmt.Sprintf("Error writing goal: %v", err))
		return
	}

	b.reply(s, m, fmt.Sprintf("Goal added for **%s**. Use `!goals` to see all.", agentName))

	// Start loop if not running
	if !b.loopMgr.IsRunning(agentName) {
		err := b.loopMgr.Start(agentName, ch.AgentDir, loop.DefaultCommandBuilder, b.sleepBetween)
		if err != nil {
			log.Printf("[keel] error starting loop for %s: %v", agentName, err)
		} else {
			b.reply(s, m, fmt.Sprintf("Agent loop started for **%s**.", agentName))
		}
	}
}

func (b *Bot) reply(s *discordgo.Session, m *discordgo.MessageCreate, msg string) {
	_, err := s.ChannelMessageSend(m.ChannelID, msg)
	if err != nil {
		log.Printf("[keel] discord send error: %v", err)
	}
}

// ParseCommand checks if a message is a ! command.
// Returns (isCommand, commandName, arguments).
func ParseCommand(content string) (bool, string, string) {
	if !strings.HasPrefix(content, "!") {
		return false, "", ""
	}
	rest := strings.TrimPrefix(content, "!")
	parts := strings.SplitN(rest, " ", 2)
	cmd := strings.TrimSpace(parts[0])
	args := ""
	if len(parts) > 1 {
		args = strings.TrimSpace(parts[1])
	}
	return true, cmd, args
}
```

Now fix the bot.go — the `lookupToken` function was broken. Let me write it properly:

```go
// internal/discord/bot.go
package discord

import (
	"fmt"
	"log"
	"os"
	"strings"
	"time"

	"github.com/bwmarrin/discordgo"

	"github.com/SeanoChang/keel/internal/config"
	"github.com/SeanoChang/keel/internal/loop"
	"github.com/SeanoChang/keel/internal/workspace"
)

type Bot struct {
	session      *discordgo.Session
	cfg          *config.Config
	loopMgr      *loop.Manager
	tailers      map[string]*LogTailer
	sleepBetween time.Duration
}

func NewBot(cfg *config.Config, sleepBetween time.Duration) (*Bot, error) {
	token := os.Getenv(cfg.Bot.TokenEnv)
	if token == "" {
		return nil, fmt.Errorf("env var %s is empty or unset", cfg.Bot.TokenEnv)
	}

	session, err := discordgo.New("Bot " + token)
	if err != nil {
		return nil, fmt.Errorf("create discord session: %w", err)
	}

	b := &Bot{
		session:      session,
		cfg:          cfg,
		loopMgr:      loop.NewManager(),
		tailers:      make(map[string]*LogTailer),
		sleepBetween: sleepBetween,
	}

	session.AddHandler(b.onMessageCreate)
	session.Identify.Intents = discordgo.IntentsGuildMessages | discordgo.IntentsMessageContent

	return b, nil
}

func (b *Bot) Start() error {
	if err := b.session.Open(); err != nil {
		return fmt.Errorf("open discord connection: %w", err)
	}
	log.Printf("[keel] Discord bot connected")

	for name, ch := range b.cfg.Channels {
		tailer := NewLogTailer(name, ch.AgentDir, ch.ChannelID, b.session)
		b.tailers[name] = tailer
		go tailer.Start()
	}

	return nil
}

func (b *Bot) Stop() {
	b.loopMgr.StopAll()
	for _, t := range b.tailers {
		t.Stop()
	}
	b.session.Close()
	log.Printf("[keel] Discord bot disconnected")
}

func (b *Bot) onMessageCreate(s *discordgo.Session, m *discordgo.MessageCreate) {
	if m.Author.ID == s.State.User.ID {
		return
	}

	agentName, ch, ok := b.cfg.ResolveChannel(m.ChannelID)
	if !ok {
		return
	}

	content := strings.TrimSpace(m.Content)
	if content == "" {
		return
	}

	if isCmd, cmd, args := ParseCommand(content); isCmd {
		b.handleCommand(s, m, agentName, ch, cmd, args)
		return
	}

	username := m.Author.Username
	if err := workspace.AppendGoal(ch.AgentDir, username, content); err != nil {
		b.reply(s, m, fmt.Sprintf("Error writing goal: %v", err))
		return
	}

	b.reply(s, m, fmt.Sprintf("Goal added for **%s**. Use `!goals` to see all.", agentName))

	if !b.loopMgr.IsRunning(agentName) {
		err := b.loopMgr.Start(agentName, ch.AgentDir, loop.DefaultCommandBuilder, b.sleepBetween)
		if err != nil {
			log.Printf("[keel] error starting loop for %s: %v", agentName, err)
		} else {
			b.reply(s, m, fmt.Sprintf("Agent loop started for **%s**.", agentName))
		}
	}
}

func (b *Bot) reply(s *discordgo.Session, m *discordgo.MessageCreate, msg string) {
	_, err := s.ChannelMessageSend(m.ChannelID, msg)
	if err != nil {
		log.Printf("[keel] discord send error: %v", err)
	}
}

// ParseCommand checks if a message is a ! command.
func ParseCommand(content string) (bool, string, string) {
	if !strings.HasPrefix(content, "!") {
		return false, "", ""
	}
	rest := strings.TrimPrefix(content, "!")
	parts := strings.SplitN(rest, " ", 2)
	cmd := strings.TrimSpace(parts[0])
	args := ""
	if len(parts) > 1 {
		args = strings.TrimSpace(parts[1])
	}
	return true, cmd, args
}
```

- [ ] **Step 4: Implement commands**

```go
// internal/discord/commands.go
package discord

import (
	"fmt"
	"strconv"
	"strings"

	"github.com/bwmarrin/discordgo"

	"github.com/SeanoChang/keel/internal/config"
	"github.com/SeanoChang/keel/internal/loop"
	"github.com/SeanoChang/keel/internal/workspace"
)

func (b *Bot) handleCommand(s *discordgo.Session, m *discordgo.MessageCreate, agentName string, ch config.ChannelConfig, cmd, args string) {
	var response string

	switch cmd {
	case "status":
		response = b.cmdStatus(agentName, ch)
	case "goals":
		response = b.cmdGoals(ch)
	case "log":
		n := 20
		if args != "" {
			if parsed, err := strconv.Atoi(args); err == nil && parsed > 0 {
				n = parsed
			}
		}
		response = b.cmdLog(ch, n)
	case "memory":
		response = b.cmdMemory(ch)
	case "stop":
		response = b.cmdStop(agentName)
	case "start":
		response = b.cmdStart(agentName, ch)
	case "clear":
		response = b.cmdClear(ch, agentName)
	default:
		response = fmt.Sprintf("Unknown command: `!%s`. Available: `!status`, `!goals`, `!log`, `!memory`, `!stop`, `!start`, `!clear`", cmd)
	}

	b.reply(s, m, response)
}

func (b *Bot) cmdStatus(name string, ch config.ChannelConfig) string {
	var sb strings.Builder
	sb.WriteString(fmt.Sprintf("**Agent: %s**\n", name))
	sb.WriteString(fmt.Sprintf("Dir: `%s`\n", ch.AgentDir))
	sb.WriteString(fmt.Sprintf("Loop running: %v\n", b.loopMgr.IsRunning(name)))

	hasGoals := workspace.HasGoals(ch.AgentDir)
	sb.WriteString(fmt.Sprintf("Has goals: %v\n", hasGoals))

	tokens, _ := workspace.MemoryTokenCount(ch.AgentDir)
	sb.WriteString(fmt.Sprintf("MEMORY.md: ~%d tokens\n", tokens))

	return sb.String()
}

func (b *Bot) cmdGoals(ch config.ChannelConfig) string {
	goals, err := workspace.ReadGoals(ch.AgentDir)
	if err != nil {
		return fmt.Sprintf("Error reading GOALS.md: %v", err)
	}
	goals = strings.TrimSpace(goals)
	if goals == "" {
		return "GOALS.md is empty."
	}
	if len(goals) > 1900 {
		goals = goals[:1900] + "\n... (truncated)"
	}
	return fmt.Sprintf("```\n%s\n```", goals)
}

func (b *Bot) cmdLog(ch config.ChannelConfig, n int) string {
	lines, err := workspace.ReadLogTail(ch.AgentDir, n)
	if err != nil {
		return fmt.Sprintf("Error reading log.md: %v", err)
	}
	if len(lines) == 0 {
		return "log.md is empty."
	}
	content := strings.Join(lines, "\n")
	if len(content) > 1900 {
		content = content[:1900] + "\n... (truncated)"
	}
	return fmt.Sprintf("```\n%s\n```", content)
}

func (b *Bot) cmdMemory(ch config.ChannelConfig) string {
	tokens, err := workspace.MemoryTokenCount(ch.AgentDir)
	if err != nil {
		return fmt.Sprintf("Error: %v", err)
	}
	return fmt.Sprintf("MEMORY.md: ~%d tokens", tokens)
}

func (b *Bot) cmdStop(name string) string {
	if !b.loopMgr.IsRunning(name) {
		return fmt.Sprintf("Agent **%s** is not running.", name)
	}
	b.loopMgr.Stop(name)
	return fmt.Sprintf("Agent **%s** stopped.", name)
}

func (b *Bot) cmdStart(name string, ch config.ChannelConfig) string {
	if b.loopMgr.IsRunning(name) {
		return fmt.Sprintf("Agent **%s** is already running.", name)
	}
	if !workspace.HasGoals(ch.AgentDir) {
		return fmt.Sprintf("Agent **%s** has no goals. Send a message first.", name)
	}
	err := b.loopMgr.Start(name, ch.AgentDir, loop.DefaultCommandBuilder, b.sleepBetween)
	if err != nil {
		return fmt.Sprintf("Error starting **%s**: %v", name, err)
	}
	return fmt.Sprintf("Agent **%s** loop started.", name)
}

func (b *Bot) cmdClear(ch config.ChannelConfig, name string) string {
	if err := workspace.ClearGoals(ch.AgentDir); err != nil {
		return fmt.Sprintf("Error clearing GOALS.md: %v", err)
	}
	return fmt.Sprintf("GOALS.md cleared for **%s**. Loop will stop after current session.", name)
}
```

- [ ] **Step 5: Implement log tailer**

```go
// internal/discord/tail.go
package discord

import (
	"log"
	"os"
	"path/filepath"
	"strings"
	"sync"

	"github.com/bwmarrin/discordgo"
	"github.com/fsnotify/fsnotify"
)

type LogTailer struct {
	agentName string
	dir       string
	channelID string
	session   *discordgo.Session
	offset    int64
	stop      chan struct{}
	once      sync.Once
}

func NewLogTailer(agentName, dir, channelID string, session *discordgo.Session) *LogTailer {
	// Initialize offset to current file size
	var offset int64
	if info, err := os.Stat(filepath.Join(dir, "log.md")); err == nil {
		offset = info.Size()
	}
	return &LogTailer{
		agentName: agentName,
		dir:       dir,
		channelID: channelID,
		session:   session,
		offset:    offset,
		stop:      make(chan struct{}),
	}
}

func (t *LogTailer) Start() {
	logPath := filepath.Join(t.dir, "log.md")
	goalsPath := filepath.Join(t.dir, "GOALS.md")

	watcher, err := fsnotify.NewWatcher()
	if err != nil {
		log.Printf("[keel] %s: fsnotify error: %v", t.agentName, err)
		return
	}
	defer watcher.Close()

	// Watch the directory (files may not exist yet)
	if err := watcher.Add(t.dir); err != nil {
		log.Printf("[keel] %s: watch error on %s: %v", t.agentName, t.dir, err)
		return
	}

	for {
		select {
		case <-t.stop:
			return
		case event, ok := <-watcher.Events:
			if !ok {
				return
			}
			if event.Has(fsnotify.Write) || event.Has(fsnotify.Create) {
				switch event.Name {
				case logPath:
					t.readNewLines(logPath)
				case goalsPath:
					t.checkGoalsEmpty(goalsPath)
				}
			}
		case err, ok := <-watcher.Errors:
			if !ok {
				return
			}
			log.Printf("[keel] %s: watcher error: %v", t.agentName, err)
		}
	}
}

func (t *LogTailer) Stop() {
	t.once.Do(func() { close(t.stop) })
}

func (t *LogTailer) readNewLines(logPath string) {
	f, err := os.Open(logPath)
	if err != nil {
		return
	}
	defer f.Close()

	info, err := f.Stat()
	if err != nil || info.Size() <= t.offset {
		return
	}

	f.Seek(t.offset, 0)
	buf := make([]byte, info.Size()-t.offset)
	n, err := f.Read(buf)
	if err != nil || n == 0 {
		return
	}
	t.offset = info.Size()

	content := strings.TrimSpace(string(buf[:n]))
	if content == "" {
		return
	}

	// Truncate if too long for Discord
	if len(content) > 1900 {
		content = content[:1900] + "\n... (truncated)"
	}

	msg := "```\n" + content + "\n```"
	_, err = t.session.ChannelMessageSend(t.channelID, msg)
	if err != nil {
		log.Printf("[keel] %s: discord send error: %v", t.agentName, err)
	}
}

func (t *LogTailer) checkGoalsEmpty(goalsPath string) {
	data, err := os.ReadFile(goalsPath)
	if err != nil {
		return
	}
	if len(strings.TrimSpace(string(data))) == 0 {
		_, _ = t.session.ChannelMessageSend(t.channelID,
			"All goals complete for **"+t.agentName+"**.")
	}
}
```

- [ ] **Step 6: Run test to verify it passes**

Run: `go test ./internal/discord/ -v`
Expected: PASS (ParseCommand test)

- [ ] **Step 7: Commit**

```bash
git add internal/discord/
git commit -m "feat: Discord bot with command routing and log tailing"
```

---

### Task 9: `keel serve --discord` command

**Files:**
- Create: `cmd/serve.go`

- [ ] **Step 1: Implement serve command**

```go
// cmd/serve.go
package cmd

import (
	"log"
	"os"
	"os/signal"
	"path/filepath"
	"strings"
	"syscall"
	"time"

	"github.com/SeanoChang/keel/internal/config"
	"github.com/SeanoChang/keel/internal/discord"
	"github.com/spf13/cobra"
)

var (
	serveConfigPath string
	serveSleep      time.Duration
)

var serveCmd = &cobra.Command{
	Use:   "serve",
	Short: "Start the Discord bot and agent loops",
	RunE:  runServe,
}

func init() {
	serveCmd.Flags().StringVar(&serveConfigPath, "config", "config/discord.toml", "Path to Discord config")
	serveCmd.Flags().DurationVar(&serveSleep, "sleep", 5*time.Second, "Sleep between agent sessions")
	rootCmd.AddCommand(serveCmd)
}

func expandPath(path string) string {
	if strings.HasPrefix(path, "~/") {
		home, _ := os.UserHomeDir()
		return filepath.Join(home, path[2:])
	}
	return path
}

func runServe(cmd *cobra.Command, args []string) error {
	cfg, err := config.Load(serveConfigPath)
	if err != nil {
		return err
	}

	// Expand ~ in agent dirs
	for name, ch := range cfg.Channels {
		ch.AgentDir = expandPath(ch.AgentDir)
		cfg.Channels[name] = ch
	}

	bot, err := discord.NewBot(cfg, serveSleep)
	if err != nil {
		return err
	}

	if err := bot.Start(); err != nil {
		return err
	}

	log.Printf("[keel] serving — press Ctrl+C to stop")

	sig := make(chan os.Signal, 1)
	signal.Notify(sig, syscall.SIGINT, syscall.SIGTERM)
	<-sig

	log.Printf("[keel] shutting down...")
	bot.Stop()
	return nil
}
```

- [ ] **Step 2: Verify it compiles**

Run: `go build -o keel . && ./keel serve --help`
Expected: Shows usage with `--config` and `--sleep` flags.

- [ ] **Step 3: Commit**

```bash
git add cmd/serve.go
git commit -m "feat: keel serve command — Discord bot with agent loops"
```

---

## Chunk 4: Polish + Integration Test

### Task 10: Fix compilation and run all tests

- [ ] **Step 1: Fix any compilation errors**

Run: `go build ./...`
Fix any issues.

- [ ] **Step 2: Run all tests**

Run: `go test ./... -v -timeout 30s`
Expected: All tests pass.

- [ ] **Step 3: Run vet and basic lint**

Run: `go vet ./...`
Expected: No issues.

- [ ] **Step 4: Commit any fixes**

```bash
git add -A
git commit -m "fix: resolve compilation and test issues"
```

---

### Task 11: Add .gitignore and CLAUDE.md

**Files:**
- Create: `.gitignore`
- Create: `CLAUDE.md`

- [ ] **Step 1: Create .gitignore**

```
# .gitignore
keel
*.exe
config/discord.toml
.env
```

- [ ] **Step 2: Create CLAUDE.md**

```markdown
# Keel

Agent loop manager and Discord bridge for the Ark of Noah ecosystem.

## Build

```bash
go build -o keel .
```

## Test

```bash
go test ./... -v
```

## Commands

- `keel run <agent>` — run a single agent loop (CLI, no Discord)
- `keel serve --discord` — start Discord bot with channel-per-agent mapping
- `keel status <agent>` — show agent status (goals, memory, log tail)

## Architecture

Single Go binary. Filesystem is the protocol — no MCP, no custom IPC.

- `internal/workspace/` — file I/O helpers for agent directories (GOALS.md, MEMORY.md, log.md, PROGRAM.md)
- `internal/agent/` — Agent struct wrapping a workspace directory
- `internal/loop/` — AgentLoop (runs `claude --agent`) + Manager (goroutine-per-agent)
- `internal/config/` — TOML config for Discord channel→agent mappings
- `internal/discord/` — Discord bot, ! commands, log.md tailing via fsnotify
- `cmd/` — Cobra CLI commands (run, serve, status)

## Config

Copy `config/discord.example.toml` to `config/discord.toml` and fill in your Discord bot token env var, guild ID, and channel-to-agent mappings.

## Agent Directory Layout

Each agent is a directory under `~/.ark/<name>/` with:
- `GOALS.md` — objectives (human adds, agent removes when complete)
- `PROGRAM.md` — instructions for how the agent should work
- `MEMORY.md` — agent-maintained working context
- `log.md` — append-only accomplishment log
- `.claude/agents/<name>.md` — Claude Code agent definition
```

- [ ] **Step 3: Commit**

```bash
git add .gitignore CLAUDE.md
git commit -m "docs: add CLAUDE.md and .gitignore"
```

---

### Task 12: End-to-end smoke test

- [ ] **Step 1: Build the binary**

Run: `go build -o keel .`
Expected: Builds successfully.

- [ ] **Step 2: Test keel status with a mock agent dir**

```bash
mkdir -p /tmp/test-ark/noah/.claude/agents
echo "## Goal\nResearch Wyckoff" > /tmp/test-ark/noah/GOALS.md
echo "Do your best." > /tmp/test-ark/noah/PROGRAM.md
echo "Some memory" > /tmp/test-ark/noah/MEMORY.md
echo "- did a thing" > /tmp/test-ark/noah/log.md
./keel status noah --dir /tmp/test-ark/noah
```

Expected: Shows agent status with goals, memory token count, and log tail.

- [ ] **Step 3: Test keel run (will fail gracefully without claude CLI available for the agent)**

```bash
./keel run noah --dir /tmp/test-ark/noah --sleep 1s
```

Expected: Starts loop, attempts to invoke `claude --agent noah`, either runs or reports session error gracefully. Ctrl+C stops it.

- [ ] **Step 4: Clean up**

```bash
rm -rf /tmp/test-ark
```

- [ ] **Step 5: Final commit with any fixes**

```bash
git add -A
git commit -m "chore: final polish for keel v0.1"
```
