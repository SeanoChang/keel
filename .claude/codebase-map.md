# Codebase Map
> Auto-generated by /map — 2026-03-12

## Stack
- Language: Go 1.26
- Framework: Cobra (CLI), discordgo (Discord bot)
- Package manager: Go modules
- Build: `go build`, GitHub Actions release workflow

## Directory Structure
```
keel/
├── main.go                     # Entry point → cmd.Execute()
├── go.mod / go.sum             # Dependencies
├── .gitignore
├── cmd/                        # Cobra CLI commands
│   ├── root.go                 # Root command, flag registration, Execute()
│   ├── run.go                  # `keel run <agent>` — single agent loop
│   ├── serve.go                # `keel serve` — Discord bot + agent loops
│   ├── status.go               # `keel status <agent>` — show agent state
│   └── update.go               # `keel update` — self-update binary
├── internal/
│   ├── agent/
│   │   └── agent.go            # Agent struct (name + dir), HasGoals()
│   ├── config/
│   │   └── config.go           # TOML config loader (BotConfig, ChannelConfig)
│   ├── discord/
│   │   ├── bot.go              # Discord Bot — message handler, session lifecycle
│   │   ├── commands.go         # !command dispatch (status/goals/log/ask/stop/start/clear)
│   │   ├── progress.go         # ProgressMessage — rate-limited in-place message editing
│   │   └── tail.go             # LogTailer — fsnotify watcher for log.md changes
│   ├── loop/
│   │   ├── loop.go             # AgentLoop — run claude sessions in a loop
│   │   ├── manager.go          # Manager — goroutine-per-agent lifecycle
│   │   └── oneshot.go          # RunOneShot / RunOneShotStreaming — single claude call
│   └── workspace/
│       └── workspace.go        # File I/O for GOALS.md, PROGRAM.md, MEMORY.md, log.md
├── config/
│   └── discord.example.toml    # Example Discord bot config
├── docs/
│   └── superpowers/plans/      # Implementation plans
└── .github/
    └── workflows/release.yml   # Cross-compile + GitHub Release on tag push
```

## Entry Points
| File | Purpose |
|------|---------|
| `main.go` | Calls `cmd.Execute()` — the only entry point |
| `cmd/root.go` | Cobra root command, registers all subcommands |
| `cmd/serve.go` | Main production entry — starts Discord bot with agent loops |
| `cmd/run.go` | Dev/CLI entry — runs a single agent loop locally |

## Key Modules

### `cmd/`
| File | Exports / Responsibility |
|------|--------------------------|
| `root.go` | `Execute()`, flag registration for all subcommands |
| `run.go` | `keel run <agent>` — resolve dir, create AgentLoop, run until goals empty |
| `serve.go` | `keel serve` — load TOML config, create Discord Bot, block on signal |
| `status.go` | `keel status <agent>` — print goals, memory tokens, log tail |
| `update.go` | `keel update` — self-update from GitHub releases |

### `internal/workspace/`
| File | Exports / Responsibility |
|------|--------------------------|
| `workspace.go` | `ReadGoals`, `HasGoals`, `AppendGoal`, `ClearGoals`, `ReadProgram`, `ReadLogTail`, `ReadMemory`, `MemoryTokenCount`, `LogSize` — all file I/O for the agent directory protocol |

### `internal/agent/`
| File | Exports / Responsibility |
|------|--------------------------|
| `agent.go` | `Agent{Name, Dir}`, `New()` validates dir exists, `HasGoals()` delegates to workspace |

### `internal/loop/`
| File | Exports / Responsibility |
|------|--------------------------|
| `loop.go` | `AgentLoop` — runs `claude --agent` in a loop: check goals → run session → archive → sleep. Streams stderr via `OnOutput`, emits lifecycle events |
| `manager.go` | `Manager` — concurrent agent lifecycle: `Start`, `Stop`, `StopAll`, `Nudge` (wake sleeping loop), `IsRunning` |
| `oneshot.go` | `RunOneShot`, `RunOneShotStreaming` — single claude invocation for `!ask` |

### `internal/config/`
| File | Exports / Responsibility |
|------|--------------------------|
| `config.go` | `Config`, `BotConfig`, `ChannelConfig` — TOML loader, `ResolveChannel()` maps Discord channel → agent, `IsAdmin()` access control |

### `internal/discord/`
| File | Exports / Responsibility |
|------|--------------------------|
| `bot.go` | `Bot` — Discord session, message routing, goal appending, auto-starts loops. `sessionHandlers()` wires ProgressMessage to lifecycle events |
| `commands.go` | `handleCommand()` — dispatches `!status`, `!goals`, `!log`, `!ask`, `!stop`, `!start`, `!clear`, `!help`, `!memory` |
| `progress.go` | `ProgressMessage` — rate-limited (2s) Discord message editing for live tool activity |
| `tail.go` | `LogTailer` — fsnotify on agent dir, sends new log.md lines + goals-complete notifications to Discord |

## Dependency Graph (simplified)
```
main.go → cmd/root
  cmd/run    → agent, loop, workspace
  cmd/serve  → config, discord
  cmd/status → agent, workspace
discord/bot      → config, loop, workspace
discord/commands → config, loop, workspace
discord/tail     → (fsnotify, discordgo)
discord/progress → (discordgo)

loop/loop    → workspace
loop/manager → loop
loop/oneshot → (os/exec)
```
